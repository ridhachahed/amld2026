---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Button from "@/components/bearnie/button/Button.astro";
import Card from "@/components/bearnie/card/Card.astro";
import CardContent from "@/components/bearnie/card/CardContent.astro";
import CardDescription from "@/components/bearnie/card/CardDescription.astro";
import CardHeader from "@/components/bearnie/card/CardHeader.astro";
import CardTitle from "@/components/bearnie/card/CardTitle.astro";

const title = "Part 1 — Introduction to OCI Cloud & MySQL AI Studio";
---

<BaseLayout title={title} description="Workshop part 1: OCI Cloud & MySQL AI Studio.">
  <main>
    <header class="border-b">
      <div class="mx-auto w-full max-w-5xl px-4 py-10">
        <p class="text-sm text-muted-foreground">Workshop content</p>
        <h1 class="mt-2 text-balance text-3xl font-semibold tracking-tight md:text-4xl">
          Introduction to OCI Cloud &amp; MySQL AI Studio
        </h1>
        <p class="mt-3 max-w-2xl text-pretty text-muted-foreground">
          Foundations for the hands-on lab: what OCI provides, how MySQL HeatWave fits in,
          and what you will use in MySQL AI Studio during the workshop.
        </p>

        <div class="mt-6 flex flex-wrap gap-3">
          <Button href={`${import.meta.env.BASE_URL}` } variant="outline">Back to workshop</Button>
          <Button href={`${import.meta.env.BASE_URL}parts/mysql-studio-tour/`} variant="default">
            Next: MySQL Studio tour
          </Button>
        </div>
      </div>
    </header>

    <section class="mx-auto w-full max-w-5xl px-4 py-14">
      <div class="grid gap-4 md:grid-cols-2">
        <Card>
          <CardHeader>
            <CardTitle>What you’ll cover</CardTitle>
            <CardDescription>High-level concepts and setup.</CardDescription>
          </CardHeader>
          <CardContent class="text-sm text-muted-foreground">
            <ul class="list-disc space-y-2 pl-5">
              <li>OCI basics and the services used in this workshop</li>
              <li>MySQL HeatWave overview: OLTP + analytics + GenAI</li>
              <li>MySQL AI Studio: where you run queries and labs</li>
            </ul>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>Outcome</CardTitle>
            <CardDescription>What you should be ready for next.</CardDescription>
          </CardHeader>
          <CardContent class="text-sm text-muted-foreground">
            You’ll know where to click, what resources are available, and how the rest of the
            workshop labs connect together.
          </CardContent>
        </Card>
      </div>
    </section>
  </main>
</BaseLayout>

